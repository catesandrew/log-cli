# ADR 0001: Copy Starter Shell

## Status

Accepted

## Decision

Seed `log-cli` from `mock-tui-platform` so the TUI starts with a working Ink root, external store pattern, and fullscreen shell.

## Consequences

- Faster implementation start
- Some copied shell files remain generic and may be simplified later
