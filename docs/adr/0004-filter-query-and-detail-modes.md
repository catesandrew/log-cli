# ADR 0004: Filter Query And Detail Modes

## Status

Accepted

## Decision

Split the UI into list mode, detail mode, filter mode, and help mode, with JSON tree folding in detail mode.

## Consequences

- Keybindings stay simple and predictable
- Filter editing does not conflict with list/detail navigation
